SUPABASE_URL=###
SUPABASE_KEY=###
# Optional. Defaults to the real OpenRouter endpoint; override only to point local e2e
# runs at a stub server (see tests/generate-approve.spec.ts).
# OPENROUTER_URL=