SUPABASE_URL=###
SUPABASE_KEY=###
# Optional. Defaults to the real OpenRouter endpoint; override only to point local e2e
# runs at a stub server (see tests/generate-approve.spec.ts). Set this in .env.e2e and
# run `npm run dev:e2e`, not here — .env.e2e is loaded automatically via Astro/Vite's
# --mode flag and is committed (it carries no secret).
# OPENROUTER_URL=