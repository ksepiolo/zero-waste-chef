---
// Legacy route from the 10x-astro-starter scaffold. The dashboard page itself is
// gone — AppNav.astro and nav/user-menu.tsx own navigation and sign-out now — but
// the URL stays alive so old bookmarks land somewhere useful instead of 404ing.
// Signed-out visitors never reach this: middleware.ts guards "/dashboard" via
// PROTECTED_ROUTES and sends them to /auth/signin first.
return Astro.redirect("/inventory");
---
